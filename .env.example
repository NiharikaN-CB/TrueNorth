# Server-side only. Never exposed to the client bundle, never committed
# with a real value (.env / .env.local are gitignored).
#
# Get a free Gemini API key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
